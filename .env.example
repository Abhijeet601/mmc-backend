SECRET_KEY=replace-with-a-strong-secret
ACCESS_TOKEN_EXPIRE_MINUTES=1440
# Railway injects DATABASE_URL automatically in production.
# Keep SQLite default locally if DATABASE_URL is not set.
DATABASE_URL=sqlite:///./mmc.db
UPLOAD_DIR=./uploads
NOTICE_SOURCE_DIR=../frontend/data files/Notice
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Default admin bootstrapped at startup if missing
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
